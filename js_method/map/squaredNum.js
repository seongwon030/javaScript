const numbers = [1, 2, 3, 4, 5];

const result = numbers.map((currentNumber) => currentNumber * currentNumber);
console.log(result);

// function calcDouble(currentNumber) {
//   return Math.pow(currentNumber, 2);
// }

// const result2 = numbers.map(calcDouble);
// console.log(result2);
